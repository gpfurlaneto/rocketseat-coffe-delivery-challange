import styled from 'styled-components'
import { mixins } from '../../styles/mixins'

export const Container = styled.p`
  margin-top: 8px;
  ${mixins.fonts.textXS};
  font-weight: 400;
  color: red;
`
